Method:                    ArrayList RunTime:    LinkedList RunTime:     Explanation:
____________________________________________________________________________________________________________
add(T elements)             0(...)                0(...)
In array list:              O(n)
The add elements methods 
will add elements to the 
end of list with two 
conditions. We check if 
the element is null to 
return false, if not it 
will create a new array
from the old one, then
add the element at the 
end of the list. Since 
the order is now changed
isSorted is updated to 
false and the size is 
incremented.

____________________________________________________________________________________________________________
pairSwap()                  0(...)                0(...)




____________________________________________________________________________________________________________
merge(List<T>otherList)     0(...)                0(...)




____________________________________________________________________________________________________________
reverse()                   0(...)                0(...)
